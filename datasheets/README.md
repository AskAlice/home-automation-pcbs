# datasheets/

Datasheet PDFs for all non-passive parts across boards/*/bom_lcsc.csv.
Passives (R/C/LED 0603/0805) are intentionally skipped.
Files: `<LCSC>_<MPN>.pdf`. See `manifest.json` for source URL and status per part.

Fetched: 39/79. Failures/unavailable are marked in the manifest (LCSC '-' rows have no LCSC listing; fetch the manufacturer PDF manually if needed).
